---
import type { IFrontmatter } from 'astro-boilerplate-components';

import { CTA } from '@/partials/CTA';
import { HeroEnglish } from '@/partials/HeroEnglish';
import { ProjectListEnglish } from '@/partials/ProjectListEnglish';
import { RecentPosts } from '@/partials/RecentPosts';
import { Sponsors } from '@/partials/Sponsors';
import BaseEnglish from '@/templates/BaseEnglish.astro';
import { AppConfig } from '@/utils/AppConfig';
import { sortByDate } from '@/utils/Posts';


const allPosts = await Astro.glob<IFrontmatter>('./posts/*.md');
const sortedPosts = sortByDate(allPosts);
const lastThreePosts = sortedPosts.slice(0, 3);

const { title } = AppConfig;
const { description } = AppConfig;
---

<BaseEnglish head={{ title, description }}>
  <HeroEnglish />
  <ProjectListEnglish />
</Base>
